// guard mechanism to prevent multiple inclusion
#ifndef Defs_H // common chosen name for guarding the file
#define Defs_H

// define PRINT1, which prints the variable a
#define PRINT1(a) printf("the variable is %c\n", a)
// define PRINT2, which prints the variables a and b
#define PRINT2(a, b) printf("the variables are %c and %c\n", a, b)

#endif